# API base URL (optional)
# VITE_API_URL=http://localhost:3000/api

# Auth mock mode: auto-on in `npm run dev`, off in production unless set to true
# VITE_AUTH_MOCK=true
# VITE_AUTH_MOCK=false

# Google OAuth client ID (optional — real Google popup when set)
VITE_GOOGLE_CLIENT_ID=
